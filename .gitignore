__pycache__/
*.pyc
*.pyo
.pytest_cache/
.coverage
htmlcov/
.DS_Store
.env
.venv/
.venv_probe/
*.egg-info/
dist/
build/
.claude/

# Internal working docs — useful locally, not meant for the public repo
# (rehearsal script, internal engineering log). Kept on disk, never pushed.
docs/DEMO_SCRIPT.md
docs/PROPOSAL.md
docs/AUDIT_REPORT.md
docs/LAG_AWARE_DIAGNOSIS.md

# GitHub Actions workflow — kept locally, not pushed. The PAT used to push
# this repo doesn't have the `workflow` scope, and GitHub rejects any push
# that adds/modifies files under .github/workflows/ without it.
.github/workflows/ci.yml

# Personal planning notes for the submission deadline — local only.
NEXT_STEPS.md